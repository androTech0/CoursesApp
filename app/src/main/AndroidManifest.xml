<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.helmy.coursesapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoursesApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".TestActivity" android:exported="true">

        </activity>
        <activity android:name=".LecturerFragments.Course.EditCourse" />
        <activity android:name=".StudentFragments.Video2Student" />
        <activity android:name=".LecturerFragments.Videos.EditVideo" />
        <activity android:name=".LecturerFragments.ViewVideos4Edit" />
        <activity android:name=".LecturerFragments.Videos.ShowVideo" />
        <activity android:name=".LecturerFragments.Videos.AddVideo" />
        <activity android:name=".LecturerFragments.Course.CourseContent" />
        <activity android:name=".LecturerFragments.Course.AddCourse" />
        <activity android:name=".Log.SignUp" />
        <activity android:name=".StudentMainActivity" />
        <activity android:name=".LecturerMainActivity" />
        <activity
            android:name=".Log.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>