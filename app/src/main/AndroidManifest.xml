<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.helmy.coursesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoursesApp">
        <activity android:name=".LecturerFragments.ShowVideo"></activity>
        <activity android:name=".LecturerFragments.AddVideo" />
        <activity android:name=".LecturerFragments.CourseContent" />
        <activity
            android:name=".Log.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Log.SignUp"
            android:exported="true" />
        <activity
            android:name=".StudentMainActivity"
            android:exported="true" />
        <activity
            android:name=".LecturerMainActivity"
            android:exported="true" />
    </application>

</manifest>